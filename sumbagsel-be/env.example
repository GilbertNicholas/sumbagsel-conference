# Database
# Format: postgresql://username:password@host:port/database
# Untuk Docker: gunakan hostname 'db' bukan 'localhost'
# Contoh: postgresql://sumbagsel_dev:sumbagsel_dev@db:5432/sumbagsel_dev
DATABASE_URL=postgresql://sumbagsel_dev:sumbagsel_dev@localhost:5432/sumbagsel_dev

# JWT Secret Key
# Gunakan string random yang kuat (minimal 32 karakter)
# Generate dengan: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# Google OAuth (Optional)
# Dapatkan dari Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Frontend URL (for CORS)
# URL frontend yang akan mengakses API ini
FRONTEND_URL=http://localhost:3001

# Node Environment
# Options: development, staging, production
NODE_ENV=development

# Port
# Port untuk API server
PORT=3000

